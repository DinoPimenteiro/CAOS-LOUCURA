// Criando com o nodejs raiz

/* import {createServer} from 'node:http'

const server = createServer((request, response) => { //Aqui será passado uma função como parâmetro
    response.write('OLÁ MUNDO CARALHO!')

    return response.end()
})

server.listen(3332) // Criando uma porta para a aplicação */
